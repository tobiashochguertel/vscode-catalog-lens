#!/usr/bin/env bash

# Modern Husky v9 - no need to source husky.sh anymore

# Colors
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
NC='\033[0m' # No Color

echo "${BLUE}🔍 Running pre-commit checks...${NC}"
echo

# STEP 1: Format code with Prettier
echo "${BLUE}✨ Step 1/7: Formatting code with Prettier...${NC}"

# First, check which files need formatting
echo "${BLUE}   Checking which files need formatting...${NC}"
FORMAT_CHECK_OUTPUT=$(pnpm format:check 2>&1)
FORMAT_CHECK_EXIT=$?

if [ $FORMAT_CHECK_EXIT -ne 0 ]; then
  echo "${YELLOW}   Files need formatting, auto-fixing...${NC}"
  echo

  # Show which files need formatting
  echo "$FORMAT_CHECK_OUTPUT" | grep -E "\[warn\]|\.md$|\.ts$|\.js$|\.json$" || true
  echo

  # Try to auto-fix
  echo "${BLUE}   Running prettier --write to auto-fix...${NC}"
  FORMAT_OUTPUT=$(pnpm format 2>&1)
  FORMAT_EXIT=$?

  if [ $FORMAT_EXIT -ne 0 ]; then
    echo
    echo "${RED}❌ Prettier formatting failed!${NC}"
    echo "${YELLOW}💡 Error details:${NC}"
    echo "$FORMAT_OUTPUT"
    echo
    echo "${YELLOW}💡 Possible causes:${NC}"
    echo "   - Syntax errors in files"
    echo "   - Files that Prettier can't parse"
    echo "   - Prettier configuration issues"
    echo
    echo "${YELLOW}💡 To fix:${NC}"
    echo "   1. Review the error messages above"
    echo "   2. Fix syntax errors manually"
    echo "   3. Run 'pnpm format' to verify"
    exit 1
  fi

  # Show which files were formatted
  echo "$FORMAT_OUTPUT" | grep -v "^>.*catalog-lens" | grep -v "^>.*prettier" | grep -E "\.md$|\.ts$|\.js$|\.json$" || true
  echo "${GREEN}✓ Files auto-formatted successfully${NC}"
  git add -u
else
  echo "${GREEN}✓ All files already formatted${NC}"
fi

# STEP 2: Lint and fix markdown
echo
echo "${BLUE}📝 Step 2/7: Linting markdown files...${NC}"

MARKDOWN_OUTPUT=$(pnpm markdown:fix 2>&1)
MARKDOWN_EXIT=$?

if [ $MARKDOWN_EXIT -ne 0 ]; then
  echo "${YELLOW}⚠️  Markdown linting found issues${NC}"
  echo "$MARKDOWN_OUTPUT" | grep -E "error|warning|\.md:" || true
  echo
  echo "${YELLOW}💡 Some markdown issues may need manual fixes${NC}"
  # Don't fail on markdown errors, just warn
else
  echo "${GREEN}✓ Markdown linting passed${NC}"
fi

git add -u

# STEP 3: Auto-fix linting issues
echo
echo "${BLUE}✨ Step 3/7: Linting and auto-fixing...${NC}"
pnpm lint:fix

# Check if auto-fix worked
if [ $? -ne 0 ]; then
  echo
  echo "${RED}❌ Lint auto-fix couldn't resolve all issues!${NC}"
  echo "${YELLOW}💡 Please fix the remaining errors manually.${NC}"
  echo "${YELLOW}💡 Run 'pnpm lint' to see all errors.${NC}"
  exit 1
fi

# Re-add auto-fixed files to staging
echo "${GREEN}✓ Auto-fix completed${NC}"
echo "${BLUE}📦 Re-staging auto-fixed files...${NC}"
git add -u

# STEP 4: Verify no lint errors remain
echo
echo "${BLUE}📋 Step 4/7: Verifying lint status...${NC}"
pnpm lint

if [ $? -ne 0 ]; then
  echo
  echo "${RED}❌ Lint errors still exist after auto-fix!${NC}"
  echo "${YELLOW}💡 Some issues require manual fixes.${NC}"
  echo "${YELLOW}💡 Please review and fix the errors above.${NC}"
  exit 1
fi

echo "${GREEN}✓ No lint errors${NC}"

# STEP 5: Type checking
echo
echo "${BLUE}✨ Step 5/7: Type checking...${NC}"
pnpm typecheck

if [ $? -ne 0 ]; then
  echo
  echo "${RED}❌ Type check failed!${NC}"
  echo "${YELLOW}💡 Fix type errors before committing.${NC}"
  exit 1
fi

echo "${GREEN}✓ Type check passed${NC}"

# STEP 6: Build
echo
echo "${BLUE}🏗️ Step 6/7: Building...${NC}"
pnpm build > /dev/null 2>&1

if [ $? -ne 0 ]; then
  echo
  echo "${RED}❌ Build failed!${NC}"
  echo "${YELLOW}💡 Run 'pnpm build' to see build errors.${NC}"
  exit 1
fi

echo "${GREEN}✓ Build successful${NC}"

# STEP 7: Validate GitHub Actions workflows (if actionlint is available)
echo
echo "${BLUE}⚙️ Step 7/7: Validating GitHub Actions workflows...${NC}"

if command -v actionlint > /dev/null 2>&1; then
  if ./scripts/lint-workflows.sh > /dev/null 2>&1; then
    echo "${GREEN}✓ Workflow validation passed${NC}"
  else
    echo
    echo "${YELLOW}⚠️  Workflow validation found issues (non-blocking):${NC}"
    ./scripts/lint-workflows.sh || true  # Prevent exit code from failing the hook
    echo
    echo "${YELLOW}💡 Fix workflow errors when convenient.${NC}"
    echo "${YELLOW}💡 Run 'pnpm workflow:lint' to see details.${NC}"
  fi
else
  echo "${YELLOW}⚠️  actionlint not installed (skipping)${NC}"
  echo "${YELLOW}💡 Install with: brew install actionlint${NC}"
fi

echo
echo "${GREEN}✅ All pre-commit checks passed!${NC}"
echo
