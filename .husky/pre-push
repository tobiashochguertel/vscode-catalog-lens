#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🚀 Running pre-push checks..."

# Run full test suite
echo "🧪 Running tests..."
pnpm test || {
  echo "❌ Tests failed!"
  exit 1
}

echo "✅ All pre-push checks passed!"
