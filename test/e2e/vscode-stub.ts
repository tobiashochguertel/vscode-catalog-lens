// Empty stub file for vscode module resolution
// The actual mock is provided by @reactive-vscode/mock via vi.mock()
// We only need to export the minimal types/enums that are imported during initialization

export enum ExtensionKind {
  UI = 1,
  Workspace = 2,
}

export {}
